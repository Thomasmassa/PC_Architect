Wekelijks rapport

Student(e):                                                       

Overzicht activiteiten

Week 6 (18/03): 12 uren:
Het aanmaken en testen van een azure database(lang aan gezeten doordat ik geen sql server kon aanmaken op german central west server)
 en het maken van een layout


Week 7 (25/03): 9 uren:
StartBuildingpage aanpassingen aangebracht verschillende manieren geprobeerd om de preset te tonen.

research gedaan naar het maken van een scraper.

Week 8 (15/04): 31 uren:
partlistview -> view aanpassingen gedaan, filter in het viewmodel aangepast en verbeterd. thuis zoeken naar een manier voor de
database images aantevullen met urls(webscraper). Bug oplossing gedaan met het dubbel laden van data in de lijst.

Alle bestanden naar nieuwe solution overgezet(bepaalde bestanden waren corrupt en usings werkte niet meer). code cleanup
Repository voor het storen van gekoze componenten, het weergeven van gekoze component in startbuildingViewModel.

swipeview voor verwijderen component.

research gedaan naar het maken van een scraper.

Week 9 (22/04): 5 uren:

bool? IsSelectedComponentFrameEnabled { get; set; } bool? IsPresetFrameEnabled { get; set; }
deze 2 propertys toegevoegd om debij de startbuild te kunnen wisselen van frame. als er een component wordt toegevoegd aan de startbuild zal de frame 
wisselen. 

foreach lus aangemaakt voor de componenten toe te voegen aan de frames

Week 10 (29/04): 13 uren:

aanpassing gemaakt bij ophalen van de data uit de database, bij opstart zal alle data opgehaald worden. RootFactory class aangemaakt, kunnen nu meerdere root instanties 
aanmaken 1 voor alle data op te slaan van de database en 1 voor de geselecteerde componenten. foreach lus aangepast in partslistviewmodel voor de nieuwe root list te 
kunnen tonen. alleen de geselecteerde lijst wordt weergeven. ook dit toegepast in de searchviewmodel zodat alle lijsten in de Root instantie worden getoond.

Propfull aangemaakt voor de image in iedere klasse als er geen image wordt meegegeven zal er een alternatief komen.

code cleanup (veriabele namen aangepast).

xaml code aangepast.

research doen voor het maken van een scraper met python voor alle andere componenten in de database vorige aanpak hiervan was te moeilijk.

Week 11 (06/05): 17 uren:

Trello aangemaakt voor de to do listss

Scraper gemaakt voor omschrijving op te halen voor elke component. filter toegevoegd voor de  partlistPage om de compatibiliteit van de compoenten (Cpu <--> Moederbord <--> Ram geheugen) te regelen. 

InternetService class zal iedere 10 seconden (op een andere thread) kijken als er internet is om de data op te halen uit de firebase.
komt steeds een Toast op het scherm die dit aangeeft (maakt gebruik van Xamarin.Essentials om de toast op de maijn thead te krijgen)

PartDeatilpage afgemaakt met Description buttin om de text te togglen

MyBuildPage, BuildDetailPage en views aangemaakt. Database repository aangemaakt. Kan vanuit de startbuild build opslaan => navigeert naar de MyBuildPage. kan naam en build verwijderen of details bekijken van de build. op de BuildDetailPage kan je de componenten bekijken van de build. de ids van de componenenten worden opgeslagen in de database dus de compoinenenten worden opgehaald uit de AllcomponentRepo. edit knop aangemaakt voor de build te editen in de StartBuildViewmodel. verwijdere knop voor de conponeneten te kunnen verwijderen uit de addedrepo zodat bij opslaan de SavedBuild class met de nieuwe id voor de zelfde priamryykey wordt opgeslagen in de db.

kan vanuit de buildDetail ook naar de partdetail voor de scpecifieke part te zien.

Fine tuning gedaan aan de code bij het inladen en verwijderen van componeneten in de startbuildviewmodel. gebruik nu .Remove i.p.v. Clear en dan alles terug laden (dit kon er haperend uitzien). Knop bij het toevoegen van storage of casefans komt er nu een knop tevoorschijn om nog extra componenten van dat type toe te voegen. 

Root class heeft niet meer de preset ingebouwd wordt nu vanuit de Viewmodel geregeld met de SetPresets() methode.

(we maken gebruik van een SqlLite db deze kan geen complexe/geneste classen opslaan, slaan de ids op met een string en vormen deze terug om naar een list<int>)


Week 12 (13/05): ..... uren:


